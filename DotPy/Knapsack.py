import sys
import os
import time


def Main():

    weight_of_bag = int(input("Enter the weight of bag: "))
    weights = list(input("Enter the weights of each item: "))
    values = list(input("Enter the profits/values of each item: "))
    