import random
import sys
import os

from tkinter import *


root = Tk()




# a = sys.argv
# root = Tk()
# root.geometry("500x500")


# # name = Label(root, text="Name").grid(row=0, sticky=E)
# # pswd = Label(root, text="Password").grid(row=1, sticky=E)

# # entry_name = Entry(root).grid(row=0, column=1)
# # entry_pswd = Entry(root).grid(row=1, column=1)

# chkbox = Checkbutton(root, text="Keep me logged in").grid(columnspan=2)

# c = ["red", "blue", "yellow", "white", "black", "blue", "orange"]
# cl = len(c)

# r1 = random.randrange(0, cl)
# r2 = random.randrange(0, cl)

# topFrame = Frame(root)
# topFrame.pack()

# bottomFrame = Frame(root)
# bottomFrame.pack(side=BOTTOM)

# submit = Button(root, text=a[1], fg=c[r1], bg=c[r2])
# r1 = random.randrange(0, cl)
# r2 = random.randrange(0, cl)

# label1 = Label(root, text=a[1], fg=c[r1], bg=c[r2])
# r1 = random.randrange(0, cl)
# r2 = random.randrange(0, cl)

# label2 = Label(root, text=a[1], fg=c[r1], bg=c[r2])
# r1 = random.randrange(0, cl)
# r2 = random.randrange(0, cl)

# label3 = Label(root, text=a[1], fg=c[r1], bg=c[r2])
# r1 = random.randrange(0, cl)
# r2 = random.randrange(0, cl)

# label3.pack(fill=NONE)
# label2.pack(fill=X)
# label1.pack(fill=Y)
# submit.pack(fill=BOTH)


root.mainloop()
